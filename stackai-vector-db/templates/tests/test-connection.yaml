apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stackai-vector-db.fullname" . }}-test-connection"
  labels:
    {{- include "stackai-vector-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stackai-vector-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
