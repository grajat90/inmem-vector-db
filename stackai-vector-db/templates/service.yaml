apiVersion: v1
kind: Service
metadata:
  name: {{ include "stackai-vector-db.fullname" . }}
  labels:
    {{- include "stackai-vector-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stackai-vector-db.selectorLabels" . | nindent 4 }}
